Name: Workflow_8059
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - main

# Required - Define action configurations.
Actions:
  Build_0f:
    # Identifies the action. Do not modify this value.
    Identifier: aws/build@v1.0.0
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
    Outputs:
      # Optional; Automatically discover reports for popular test frameworks
      AutoDiscoverReports:
        Enabled: true
        # Use as prefix for the report files
        ReportNamePrefix: rpt
    # Defines the action's properties.
    Configuration:
      # Required - Steps are sequential instructions that run shell commands
      Steps:
        - Run: npm install
        - Run: npm run build
        - Run: mkdir -p rent-a-room/build
        - Run: mv build/* rent-a-room/build/
    Compute:
      Type: Lambda
    Environment:
      Name: production
  DeploytoAWSAmplifyHosting_ee:
    # Identifies the action. Do not modify this value.
    Identifier: codecatalyst-labs/deploy-to-amplify-hosting@v1.0.1
    # Required; You can use an environment, AWS account connection, and role to access AWS resources.
    Environment:
      Connections:
        - Role: CodeCatalystWorkflowDevelopmentRole-WebDevTeam
          Name: DevEnvironment
      Name: production
    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Optional
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source
    Compute:
      Type: EC2
    Configuration:
      Wait: true
      CreateBranch: true
      AmplifyBranchName: ${WorkflowSource.BranchName}
      Path: rent-a-room/build
    DependsOn:
      - Build_0f
